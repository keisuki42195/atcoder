#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

double min1(double a, double b, double c) {
  if(a/2 > b/3){
    if(c/5 > b/3){
      return b
  }
  if(a/2 < b/3 || a/2 < c/5){
    return a;
  }
  return c; 
}

int main()
{
  int t, m;
  scanf("%d", &t);
  for (int i = 0; i < t; i++){
    int n, a, b, c, d;
    scanf("%d%d%d%d%d", &n, &a, &b, &c, &d);
    int x = 1;
    int p = d;

     min
    while(){
      x = x*5;
      p = p+c;
      if(x > n){
        p = p+d*(x-n);

      }

    }
  }


  printf("%.20lf\n", l);
  return 0;
}